export enum EmitType {
    GET_FEATURES = "getFeatures",
    GET_LAST_UPDATE_TRACKER = "getLastUpdateTracker"
}

export enum EventType {
    GET_FEATURES = "getFeaturesResponse",
    GET_LAST_UPDATE_TRACKER = "lastUpdateTrackerResponse"
}